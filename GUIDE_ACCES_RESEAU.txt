╔════════════════════════════════════════════════════════════════════╗
║   GUIDE : PERMETTRE L'ACCÈS À DISTANCE VIA ADRESSE IP            ║
╚════════════════════════════════════════════════════════════════════╝

Ce guide explique comment configurer l'application EOS pour que d'autres 
ordinateurs sur le réseau puissent y accéder via l'adresse IP.

═══════════════════════════════════════════════════════════════════════
ÉTAPE 1 : TROUVER L'ADRESSE IP DU SERVEUR
═══════════════════════════════════════════════════════════════════════

1. Sur l'ordinateur où l'application est installée (le serveur), 
   ouvrez PowerShell et tapez :

   ipconfig

2. Cherchez "Adresse IPv4" dans la section "Ethernet" ou "Wi-Fi"
   
   Exemple : 192.168.1.100

3. NOTEZ CETTE ADRESSE IP, vous en aurez besoin !

═══════════════════════════════════════════════════════════════════════
ÉTAPE 2 : CONFIGURER LE PARE-FEU WINDOWS (SUR LE SERVEUR)
═══════════════════════════════════════════════════════════════════════

Pour permettre l'accès externe, vous devez autoriser les ports dans le 
pare-feu Windows.

1. Appuyez sur Windows + R
2. Tapez : wf.msc
3. Appuyez sur Entrée

4. Cliquez sur "Règles de trafic entrant" (à gauche)
5. Cliquez sur "Nouvelle règle..." (à droite)

6. Créez 2 règles (une pour le Backend, une pour le Frontend) :

   RÈGLE 1 : BACKEND (Port 5000)
   ─────────────────────────────
   - Type de règle : Port
   - Protocole : TCP
   - Port spécifique : 5000
   - Action : Autoriser la connexion
   - Profils : Domaine, Privé, Public (cochez les 3)
   - Nom : EOS Backend (Port 5000)

   RÈGLE 2 : FRONTEND (Port 5173)
   ──────────────────────────────
   - Type de règle : Port
   - Protocole : TCP
   - Port spécifique : 5173
   - Action : Autoriser la connexion
   - Profils : Domaine, Privé, Public (cochez les 3)
   - Nom : EOS Frontend (Port 5173)

═══════════════════════════════════════════════════════════════════════
ÉTAPE 3 : VÉRIFIER LA CONFIGURATION DU BACKEND (SUR LE SERVEUR)
═══════════════════════════════════════════════════════════════════════

Le backend est déjà configuré pour écouter sur toutes les interfaces 
(0.0.0.0), donc il n'y a RIEN À FAIRE.

Pour vérifier, ouvrez : D:\EOS\backend\app.py

Vous devriez voir à la fin du fichier :
   app.run(host='0.0.0.0', port=5000, debug=True)

Si c'est bien le cas, tout est OK !

═══════════════════════════════════════════════════════════════════════
ÉTAPE 4 : CONFIGURER LE FRONTEND (SUR LE SERVEUR)
═══════════════════════════════════════════════════════════════════════

Le frontend est déjà configuré pour détecter automatiquement l'adresse 
IP du client, donc il n'y a RIEN À FAIRE de ce côté non plus !

Le fichier D:\EOS\frontend\src\config.js utilise :
   API_URL: `http://${window.location.hostname}:5000`

Cela signifie que le frontend utilisera automatiquement l'adresse IP
depuis laquelle l'utilisateur se connecte.

═══════════════════════════════════════════════════════════════════════
ÉTAPE 5 : DÉMARRER L'APPLICATION (SUR LE SERVEUR)
═══════════════════════════════════════════════════════════════════════

1. Ouvrez PowerShell dans D:\EOS
2. Exécutez :

   .\DEMARRER_EOS_SIMPLE.bat

3. Attendez que les deux services démarrent :
   - Backend : http://0.0.0.0:5000
   - Frontend : http://0.0.0.0:5173

4. Le Backend devrait afficher :
   * Running on all addresses (0.0.0.0)
   * Running on http://127.0.0.1:5000
   * Running on http://192.168.X.X:5000

5. Le Frontend devrait afficher :
   ➜  Local:   http://localhost:5173/
   ➜  Network: http://192.168.X.X:5173/

═══════════════════════════════════════════════════════════════════════
ÉTAPE 6 : SE CONNECTER DEPUIS UN AUTRE ORDINATEUR
═══════════════════════════════════════════════════════════════════════

1. Sur l'autre ordinateur (le client), ouvrez un navigateur web

2. Tapez l'adresse IP du serveur avec le port 5173 :

   http://192.168.X.X:5173
   
   (Remplacez 192.168.X.X par l'adresse IP du serveur trouvée à l'étape 1)

3. L'application EOS devrait s'ouvrir !

4. Connectez-vous avec vos identifiants habituels

═══════════════════════════════════════════════════════════════════════
RÉSOLUTION DE PROBLÈMES
═══════════════════════════════════════════════════════════════════════

❌ PROBLÈME 1 : "Cette page n'est pas accessible"
─────────────────────────────────────────────────

Solution :
1. Vérifiez que le pare-feu est bien configuré (Étape 2)
2. Vérifiez que l'application est démarrée sur le serveur
3. Vérifiez que les deux ordinateurs sont sur le même réseau
4. Essayez de "pinger" le serveur depuis le client :
   
   ping 192.168.X.X
   
   Si ça ne répond pas, le problème vient du réseau.

❌ PROBLÈME 2 : "Network Error" ou "CORS Error"
────────────────────────────────────────────────

Solution :
1. Ouvrez D:\EOS\backend\config.py
2. Ajoutez l'adresse IP du client dans la liste CORS :

   CORS_ORIGINS = os.environ.get('CORS_ORIGINS', 
     'http://localhost:5173,http://192.168.X.X:5173').split(',')
   
   Ajoutez autant d'adresses IP que nécessaire, séparées par des virgules.

3. Redémarrez le backend

❌ PROBLÈME 3 : Le client Sherlock n'apparaît pas
──────────────────────────────────────────────────

Solution :
1. Sur le serveur, exécutez : SYNCHRONISER_VERS_AUTRE_ORDI.bat
2. Copiez les 5 fichiers SQL générés sur l'autre ordinateur
3. Sur l'autre ordinateur, exécutez : IMPORTER_DEPUIS_AUTRE_ORDI.bat
4. Redémarrez l'application

❌ PROBLÈME 4 : Les deux ordinateurs ont des IP différentes (normal !)
──────────────────────────────────────────────────────────────────────

C'est NORMAL ! Chaque ordinateur a sa propre adresse IP.

- Ordinateur SERVEUR (où l'application tourne) : 192.168.1.100
- Ordinateur CLIENT (qui se connecte) : 192.168.1.101

Pour se connecter :
1. Le CLIENT doit utiliser l'IP du SERVEUR
2. Dans le navigateur du CLIENT, tapez : http://192.168.1.100:5173

Le frontend détecte automatiquement quelle IP utiliser pour le backend !

═══════════════════════════════════════════════════════════════════════
RÉCAPITULATIF RAPIDE
═══════════════════════════════════════════════════════════════════════

✅ SUR LE SERVEUR (ordinateur principal) :
   1. Ouvrir les ports 5000 et 5173 dans le pare-feu
   2. Trouver l'adresse IP : ipconfig
   3. Démarrer l'application : DEMARRER_EOS_SIMPLE.bat

✅ SUR LE CLIENT (autre ordinateur) :
   1. Ouvrir le navigateur
   2. Aller sur : http://[IP_DU_SERVEUR]:5173
   3. Se connecter normalement

✅ POUR SYNCHRONISER LES DONNÉES (incluant Sherlock) :
   1. Sur le serveur : SYNCHRONISER_VERS_AUTRE_ORDI.bat
   2. Copier les 5 fichiers SQL vers l'autre ordinateur
   3. Sur l'autre ordinateur : IMPORTER_DEPUIS_AUTRE_ORDI.bat

═══════════════════════════════════════════════════════════════════════
NOTES IMPORTANTES
═══════════════════════════════════════════════════════════════════════

📌 L'application est déjà configurée pour l'accès réseau !
   - Backend : écoute sur 0.0.0.0 (toutes les interfaces)
   - Frontend : détecte automatiquement l'IP
   - CORS : supporte plusieurs IP

📌 Vous n'avez besoin de configurer QUE le pare-feu Windows !

📌 Si vous changez de réseau, l'adresse IP peut changer.
   Refaites l'étape 1 pour trouver la nouvelle IP.

📌 Pour un accès permanent, configurez une IP fixe sur votre routeur
   (recherchez "DHCP Reservation" dans les paramètres de votre routeur)

═══════════════════════════════════════════════════════════════════════

